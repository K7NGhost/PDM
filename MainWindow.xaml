<Window x:Class="PDM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PDM" xmlns:viewmodels="clr-namespace:PDM.Src.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:DatabaseManagerViewModel}"
        mc:Ignorable="d"
        Title="Phone Database Manager" Height="650" Width="1200">
    <Window.Resources>
        
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="{DynamicResource BackgroundSecondaryBrush}" BorderThickness="0,0,0,1" BorderBrush="{DynamicResource TextNormalBrush}">
            <Menu>
                <MenuItem Header="File">
                    <MenuItem Header="Create New DB" Command="{Binding CreateNewCommand}"></MenuItem>
                    <MenuItem Header="Open DB" Command="{Binding OpenCommand}"></MenuItem>
                </MenuItem>
            </Menu>
        </Border>

        <Border Grid.Row="1" Background="{DynamicResource BackgroundPrimaryAltBrush}" BorderBrush="{DynamicResource TextNormalBrush}" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Border BorderBrush="{DynamicResource TextNormalBrush}" BorderThickness="0,0,1,0">
                    <StackPanel Orientation="Vertical">
                        <Button Content="Dashboard" HorizontalAlignment="Stretch" Click="NavToDashboardPage_Click" Margin="0,0,0,10"/>
                        <Button Content="Phone List" HorizontalAlignment="Stretch" Click="NavToPhoneListPage_Click" Margin="0,0,0,10"></Button>
                        <Button Content="Add Phone" HorizontalAlignment="Stretch" Click="NavToAddPhonePage_Click" Margin="0,0,0,10"></Button>
                        <Button Content="Settings" HorizontalAlignment="Stretch" Margin="0,0,0,10"></Button>
                        <Button Content="Import/Export" HorizontalAlignment="Stretch" Margin="0,0,0,10"></Button>
                    </StackPanel>
                </Border>

                <Frame Grid.Column="1" x:Name="MainFrame"></Frame>
                
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="{DynamicResource BackgroundSecondaryBrush}" BorderBrush="{DynamicResource TextNormalBrush}" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10">
                    <TextBlock Text="Current Database Loaded: "></TextBlock>
                    <TextBlock Text="{Binding Path}" FontWeight="Bold"></TextBlock>
                </StackPanel>

                <StackPanel Margin="10" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="Back" Click="NavToPrevPage_Click" Margin="0,0,10,0"></Button>
                    <Button Content="Next" Click="NavToNextPage_Click"></Button>
                </StackPanel>
            </Grid>
            
        </Border>
        
    </Grid>
</Window>
